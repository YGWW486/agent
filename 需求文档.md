# 需求文档（v0.2）

> 本文档由产品选择题结果整理而成，用于约束 MVP 与后续迭代范围。  
> 修订时请更新版本号与「修订记录」。

---

## 1. 产品概述

### 1.1 定位

- **本地优先**的 Agentic Engineering 控制台：多 Agent 在同一工作区内协作完成编码类任务，强调**流程可见、合入前人工审批、可长时间运行**。
- 后端为 **FastAPI + LangGraph** 编排；桌面端为 **Electron**，负责一键启停本地服务、提交任务、订阅状态、审批与查看产出。

### 1.2 目标用户（当前）

- 首要：**作者自用**；架构与能力按**可演进至企业级**预留（审计、治理等可在后续版本补充）。

### 1.3 成功标准（MVP）

- 在 Windows 上通过 **绿色 zip** 解压后，可完成：**选工作区 → 提交 spec → 观察流水线节点状态（推送）→ 人工审批 → 查看代码/diff**。
- 本地 API 仅监听 **127.0.0.1**；密钥走 **系统凭据** 存储。
- **无硬性发布截止**，以**质量优先**为节奏原则。

---

## 2. 范围与非目标

### 2.1 MVP 必须包含（界面）

| 模块 | 说明 |
|------|------|
| 工作区/项目管理 | 选择本地目录；支持**多项目/多配置**切换；项目间上下文与记忆**隔离**。 |
| 提交 spec | 输入需求说明（及后续可扩展的模板/附件，首版以文本为主）。 |
| 运行时间线 | 展示 Planner → Coder → Reviewer → (Approval) 节点状态，**串行 DAG 执行**（MVP 不做并行）；**状态通过 SSE 或 WebSocket 推送**更新。 |
| 审批面板 | **Merge 前必须人工**「通过 / 拒绝」；拒绝须可操作（备注等具体字段可在详细设计里定）。 |
| 产出预览 | 代码与 **diff** 的只读或简单预览（编辑深度另见详细设计）。 |
| 设置 | API Key、模型相关、本地服务端口等可配置项。 |

### 2.1.1 Pipeline 节点契约（MVP）

每个节点的**输入/输出类型**在编码前必须明确定义。以下是 MVP 最低要求：

| 节点 | 输入 | 输出 | 失败策略 |
|------|------|------|----------|
| **Planner** | 用户 Spec 原文 + 项目索引（graphify） | Task DAG（每任务 ≤15 min 工作量、文件范围、验收条件、依赖关系） | 返回错误描述；人工修正 Spec 后重试 |
| **Coder** | 单个 Task 定义 + 相关文件上下文 | 代码变更（diff）+ 自检报告（是否通过验收条件） | 打回 Planner 重拆该 Task（最多 2 次）；仍失败则挂起等人 |
| **Reviewer** | Coder diff + 自检报告 + Task 验收条件 | Review 结论（通过/拒绝）+ 理由 + 附带的测试用例 | Review 不通过 → 打回 Coder（含具体问题描述），不做自动重试 |

### 2.1.2 质量验证门槛

编码节点必须产出可验证的证据，审批才有依据：

1. **Coder 自检**：Coder 必须在输出中声明每个验收条件的满足情况（满足/未满足/不确定）。
2. **Reviewer 附带测试**：Reviewer 的 review 结论必须附带至少一个可运行的测试用例（单元测试或脚本），用于验证关键路径。
3. **人工审批只看验证机制**：审批界面应展示 Coder 自检结果 + Reviewer 结论 + 附带测试的运行结果，人类据此判断"验证是否到位"，而非逐行审代码。

### 2.1.3 任务拆解约束

Planner 产出的 Task DAG 必须遵循（详见 `Agentic-Engineering-指南.md` 第六节）：

- 每 Task **≤ 15 分钟**可完成的工作量
- 每 Task **独立可验证**，有自己的验收条件
- 同一 DAG 层级内的 Task 操作的文件集**不相交**（避免编辑冲突）
- Task 之间依赖关系明确，形成有向无环图（DAG）

### 2.1.4 失败处理路径

| 场景 | MVP 行为 |
|------|---------|
| Coder 执行失败（超时/异常） | 自动重试同一 Task 1 次；仍失败 → 挂起，在时间线标红，等人介入 |
| Reviewer 拒绝 | 带问题描述打回 Coder，Coder 修正后重新提交 review；`MAX_REVISIONS=0` 时不限轮次，`>0` 时达上限挂起 |
| 修订轮次耗尽 | `MAX_REVISIONS>0` 且 Reviewer 连续 REJECT 达上限 → 挂起，`failure_reason` 说明原因 |
| Token 预算 | 单任务超 `TASK_TOKEN_LIMIT` → 挂起；日累计超 `DAILY_TOKEN_BUDGET` → 拒绝启动新 workflow（HTTP 429） |
| Planner 拆解不合理（Coder 连续 2 个 Task 失败） | 挂起，提示用户修改 Spec 或手动调整任务拆分 |
| 外部 API 不可用 | 不自动降级模型；显示错误状态，等人手动切换配置 |
| Reviewer LLM 失败 | `status=failed`，终止流程，不进入 revise 环 |
| 人工审批拒绝（Merge 前） | 打回 Coder，使用合法 `ReviewResult`（`source=human`），**不计入** Reviewer 修订轮次 |

### 2.2 MVP 日志策略（待详细设计时定稿）

- 选择题中**未勾选**「独立可搜索日志页」。
- **暂定**：首版以**时间线 + 节点内展开片段**为主，满足排障；若开发中发现不足，可将「全文日志页」提升为 MVP 内项。

### 2.3 明确不做（MVP）

- **内置 Git 工作流**（提交/推送等）：Git 仍使用**外部工具**；桌面可不展示 `git status`（除非后续版本增加）。
- **非 Windows** 首发支持（macOS/Linux 列为后续）。
- **Merge 自动通过**：策略为**永远人工**；自动通过类能力不在 MVP 范围。
- **并行 Agent 执行**：MVP 采用**串行 DAG**，先验证单 Agent 流程的可靠性。并行编排列为后续迭代。
- **自动重试超过 1 次**：Coder 失败最多自动重试 1 次，超出后挂起等人介入，不做无限重试。

---

## 3. 平台与技术栈

| 项 | 决策 |
|----|------|
| 操作系统 | **仅 Windows**（首发） |
| 桌面框架 | **Electron** |
| 后端集成 | 桌面 **一键启动/停止** 内置（或打包）**Python 子进程**承载 FastAPI，退出策略需在实现阶段定义（避免僵尸进程）。 |
| 安装形态 | **绿色 zip**，解压即用 |
| 本地服务绑定 | **仅 127.0.0.1**，禁止默认对外开放局域网。 |

---

## 4. 模型与网络

| 项 | 决策 |
|----|------|
| 调用方式 | 允许 **公网 API**（如 DeepSeek 等云端）；不要求 MVP 内「完全离线本地推理」。 |
| 密钥 | 使用 **操作系统凭据/钥匙串** 存储 API Key，禁止明文写入仓库。 |

（按节点路由不同模型/密钥等能力可在《技术设计》中展开，本需求文档仅约束「允许多配置、可扩展」。）

---

## 5. 知识索引与 Token（类 graphify）

> **选型已定（2026-05-05）。** 见下表。

| 项 | 决策 |
|----|------|
| 实现路径 | **集成既有 graphify CLI + 轻量封装**。后端通过子进程调用 graphify CLI，解析产物 `graph.json`，存入内存/SQLite 作为查询数据源。不做 MCP Server、不做文件监听。 |
| 索引触发 | **手动触发**。用户在提交 spec 前或需要时通过桌面端按钮发起 `POST /api/index/rebuild`，后端异步执行并返回进度。 |
| 查询接口 | `GET /api/context?files=x&depth=2` — 根据文件列表返回子图节点、摘要、关联文件。Planner/Coder 节点在需要上下文时调用此接口。 |
| 存储 | MVP 使用**内存字典 + SQLite 持久化缓存**。图数据在服务启动时从 graph.json 加载到内存，查询走内存；SQLite 仅存最近一次索引产物的完整快照，用于重启免重建。 |

---

## 6. 对话与记忆

| 项 | 决策 |
|----|------|
| 聊天记录压缩 | **可配置开关**；支持 **手动触发** 生成摘要（用于长会话）。自动按阈值压缩列为后续可选。 |
| 长期记忆 | **仅项目级**：与仓库/项目绑定的约定、模块边界、已确认决策等；**不做**全局用户偏好记忆（除非未来版本单独立项）。 |

与 **LangGraph checkpoint**（工作流运行状态）的关系：checkpoint 负责「跑到哪」；项目记忆负责「项目长期知识」，二者在设计上需区分存储与生命周期。

---

## 7. 安全与合规（MVP 级）

- 本地 HTTP **仅本机回环**。
- 文件与命令执行需与《实施规划》中 **工作区沙箱、白名单** 方向一致（具体规则在技术设计/实现中落地）。
- 敏感文件（如 `.env`）默认不可由 Agent 工具读取或需在策略中显式禁止。

---

## 8. 依赖与文档

- 编排与阶段路线参见同仓库 `实施规划.md`。
- API 契约（REST + 推送通道）需在《接口说明》中单列版本与兼容策略。

---

## 9. 修订记录

| 版本 | 日期 | 说明 |
|------|------|------|
| v0.1 | 2026-05-04 | 初版：由交互选择题结果整理；日志策略与 graphify 实现路径标为待设计定稿。 |
| v0.2 | 2026-05-05 | 补充 Pipeline 节点契约、质量验证门槛、任务拆解约束、失败处理路径；graphify 提升为阻塞性决策；明确 MVP 串行 DAG 策略。 |
| v0.3 | 2026-05-05 | graphify 选型落地：集成既有 CLI + 轻量封装，手动触发，内存+SQLite 存储，API 接口定型。 |
